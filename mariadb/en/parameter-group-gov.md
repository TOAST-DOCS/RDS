<!-- machine_translated: true -->

<!-- pre-align:aligned sig=81b50379a112 -->

<a id="database-rds-for-enginepascalcase-parameter-group"></a>
## Database > RDS for MariaDB > Parameter Group { #database-rds-for-enginepascalcase-parameter-group }

<a id="parameter-group"></a>
## Parameter Group { #parameter-group }

RDS for MariaDB provides a parameter group feature to apply the settings from MariaDB installed on DB instances. A parameter group is a set of parameters that allow you to configure MariaDB. When the service is activated, a default parameter group is provided for each version of all DB engines. The default parameter group is provided as `default.{DB engine version name}` and consists of the recommended default parameter values for each version. A default parameter group for each DB engine version family is also provided. The default parameter group can be modified or deleted in the same way as a regular parameter group.

<a id="parameter-group-type"></a>
### Parameter Group Type { #parameter-group-type }

Parameter groups are divided into two types based on their scope of application.

| Type | Scope | Default Parameter Group Name |
|---------|--------------------------------------|-------------------------|
| Family (recommended) | DB instances of all minor versions belonging to the same major version | `default.{DB engine version family}` |
| Single | DB instances using a specific DB engine version | `default.{DB engine version name}` |

A DB engine version family is a collection of DB engine versions that correspond 1:1 to a major version. All minor versions that share the same major version belong to a single DB engine version family.

Using a family parameter group allows you to apply the same parameter group to DB instances with different minor versions, eliminating the need to create and manage a separate parameter group for each minor version. For more information, see [Family Parameter Group](parameter-group-gov/#family-parameter-group).

<a id="create-parameter-group"></a>
### Create Parameter Group { #create-parameter-group }

You can create a parameter group in the parameter console as needed. When creating a parameter group, first select the parameter group type. If you select Family, specify the DB engine version family; if you select Single, specify the DB engine version. After creation, the type cannot be changed.

The parameter group name has the following constraints.

* Must be unique for each region.
* Must contain alphabets, numbers, and - _ between 1 and 100 characters, and the first character must be an alphabet.

When you create a parameter group, the parameter is always generated by default. To create based on an existing parameter group, you must create a parameter group using the copy parameter feature.

<a id="copy-parameter-group"></a>
### Copy Parameter Group { #copy-parameter-group }

Creates a new parameter group based on an existing parameter group. The copied parameter group consists of the parameter values of the source parameter group and is created with the same type as the source. A family parameter group can only be copied as a family parameter group. There is no association between the source parameter group and the copied parameter group, and any changes or deletions made to the source parameter group do not affect the copied parameter group.

<a id="reset-parameter-group"></a>
### Reset Parameter Group { #reset-parameter-group }

When you reset the parameter group, you change the values of all parameters to the default values for the DB engine version.

<a id="apply"></a>
### Apply Parameter Group { #apply }

You can select a parameter group to apply to a DB instance when creating or modifying the DB instance. One parameter group is applied to one DB instance, and one parameter group can be applied to multiple DB instances. The selection list displays both single parameter groups that match the DB engine version of the DB instance and family parameter groups for the DB engine version family to which that version belongs. When a parameter in a parameter group is changed, the change is not applied to the DB instance immediately. If there are associated DB instances, the parameter group status changes to `Apply Required`. On the DB Instance List screen, select a DB instance associated with the parameter group and click **Apply Parameter Group Changes** to apply the parameter changes to the DB instance. When the parameter group changes are applied to all associated DB instances, the parameter group status changes to `Apply Completed`.

!!! danger "Caution"
    If the parameters that require restart have changed, the DB instance will be restarted during applying changes.

<a id="compare-parameter-group"></a>
### Compare Parameter Group { #compare-parameter-group }

After selecting two different parameter groups in the console, click the **Compare** button to see what parameters are different. You can compare parameter groups for different DB engine versions as well as the same DB engine version.

<a id="delete-parameter-group"></a>
### Delete Parameter Group { #delete-parameter-group }

You are free to delete parameter groups except those already applied to the DB instances. To delete a parameter group already applied to a DB instance, you must first change the parameter group of all connected DB instances before you delete it.

<a id="family-parameter-group"></a>
## Family Parameter Group { #family-parameter-group }

A family parameter group is a parameter group that can be applied to DB instances of all minor versions belonging to the same DB engine version family — that is, the same major version. If you use the same major version, you can manage all instances with a single parameter group even if their minor versions differ, and you don't need to reselect a parameter group when you upgrade the minor version.

!!! tip "Note"
    [Auto Minor Version Upgrade](db-instance-gov/#maintenance-auto-minor-version-upgrade) applies only to DB instances that use a family parameter group.

<a id="template-range"></a>
### Template Range { #template-range }

Even for the same parameter, the default value or allowed values may differ depending on the minor version. A family parameter group expresses these differences as template ranges. A template range is a grouping of minor versions that share the same default value, allowed values, data type, and apply type, and is displayed as a DB engine version range on the parameter group details screen.
For example, it is displayed as `MariaDB 10.11.7 ~ MariaDB 10.11.13`.

* Parameters whose values do not vary by minor version have a single range, and the same value applies to all minor versions.
* Parameters whose values vary by minor version are divided into multiple ranges, and a different value can be specified for each range.
* Template ranges are calculated automatically from the DB engine version information. Users cannot directly specify or change ranges.

<a id="change-parameter-by-range"></a>
### Modify Parameters by Range { #change-parameter-by-range }

Parameters in a family parameter group are modified in units of template ranges. Any range whose values have not been modified uses the default values of the corresponding minor version as-is. Therefore, even when a new minor version is added, the modified values are retained, and parameters that have not been modified follow the default values of the new minor version.

The values of the range to which its own DB engine version belongs are applied to the DB instance.

<a id="compare-family-parameter-group"></a>
### Compare Family Parameter Group { #compare-family-parameter-group }

A family parameter group compares values within the same range segment. When comparing based on a specific DB instance, only the range segment that the DB instance's DB Engine version belongs to is compared.

<a id="default-family-parameter-group"></a>
### Default Family Parameter Group { #default-family-parameter-group }

When the service is activated, a default family parameter group is provided for each DB engine version family under the name `default.{DB engine version family}`. All parameters are configured with the default values for each minor version, and the default family parameter group can be modified or deleted in the same way as a regular family parameter group.

<a id="parameter"></a>
## Parameter { #parameter }

The parameter contains the following information.

| Item             | Description                                                                                                                                                                                                       | 
|------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------| 
| Group            | Option group of option file (my.cnf).                                                                                                                                                                             | 
| Name             | Option name of option file (my.cnf).<br/>When the option name and system variables (System Variables) are different, they are additionally displayed in the format (**_- Variable_**: `System Variables`).         |
| Template Range | The range of DB engine versions to which the value applies.<br/>Displayed only for family parameter groups. |
| Value            | Value to be applied to parameters.                                                                                                                                                                                |
| Allowed values   | Range of values applicable to parameters.<br/>                                                                                                                                                                    | 
| Application Type | Either `Static` or `Dynamic`.<br/>For `Static`, the DB instance must be restarted to apply the parameter changes.<br/>For `Dynamic`, the parameters are applied immediately without restarting the DB instance. |
| Data Format      | Format of the parameter value.                                                                                                                                                                                    |

<a id="parameter-variables-formulas-and-functions"></a>
### Parameter Variables, Formulas, and Functions { #parameter-variables-formulas-and-functions }

Certain parameters may be better represented by formulas using values associated with DB instances rather than using fixed values. To support this, you can use predefined variables, formulas, and functions for `NUMERIC`, `INTEGER` data format.

* Formulas
    * You can use `()`, `+`, `-`, `*`, `/`.
    * The result of a formula must always be a number.
    * If the data type is `INTEGER`, decimals are discarded.
    * If the data type is `NUMERIC`, it is rounded to the ninth decimal place.
* Functions
    * `max(a, b, ...)`: Returns the largest of several values.
    * `min(a, b, ...)`: Returns the smallest of several values.
    * `sum([a, b, ...])`: Returns the sum of multiple values.
* Variable
    * `ramSizeByte`: Indicates the byte value of the memory size of the current DB instance type.
    * `vCPU`: Indicates the number of virtual CPU cores for the current DB instance type.
    * `dbPort`: Indicates the DB port of the current DB instance.
    * `serverId`: Indicates the server ID assigned to the current DB instance.
    * `readOnly`: Indicates `1` or `0` when the current DB instance is read-only.

Below example is the default value of `innodb_buffer_pool_size` parameter, and it indicates setting to 6/10 of the memory size of the DB instance type.

``` 
ramSizeByte * 6 / 10 
```

<a id="change-parameter"></a>
### Change Parameter { #change-parameter }

You can change the parameters by selecting a parameter group from the console and pressing the **Edit Parameters** button. For parameters that cannot be changed, the value appears in plain text, and for parameters that can be changed, the INPUT that can be changed appears. When you press the `Preview Changes` button on the edit screen, a separate pop-up screen will be displayed to view the changed parameters, press the `Reset` button to return to the time before the change. All changes made
in edit mode are reflected in the parameter group by pressing the `Save Changes` button. For information about reflecting DB instances of changed parameter groups, refer to the [Apply Parameter Groups](parameter-group-gov/#apply).

